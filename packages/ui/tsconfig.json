{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "outDir": "./dist",
        "rootDir": "./src",
        "composite": true,
        "declaration": true,
        "noEmit": false,
        "lib": ["DOM", "DOM.Iterable", "ESNext"],
        "types": [] // Explicitly empty to avoid implicit Node types
    },
    "include": [
        "src/**/*.ts",
        "src/**/*.tsx"
    ]
}